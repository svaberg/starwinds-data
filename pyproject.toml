[project]
name = "starwinds_data"
version = "0.1.0"
description = "A package for working with Starwinds data"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where=["src"]

